name = "cloudbasega-forms"
main = "forms-worker.js"
compatibility_date = "2026-07-01"

# Secrets (set with `npx wrangler secret put <NAME>`):
#   RESEND_API_KEY  - API key from resend.com (verify the cloudbasega.com domain there first)
#
# Optional plain-text vars:
# [vars]
# MAIL_FROM = "CloudBaseGA Website <forms@cloudbasega.com>"
# MAIL_TO = "info@cloudbasega.com, ben@cloudbasega.com"
